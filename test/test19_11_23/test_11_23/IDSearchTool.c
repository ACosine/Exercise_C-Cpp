#include "common.h"
int main(int argc, char *argv[])
{
	const char *title = "身份验证查询系统";   //全局变量title变量
	DrawFrame(title);                         //画整体框架

	char cmd[MAX_CMD_SIZE] = { 0 };
	//while(1)
	{
		//DrawFrame(title);
	}

	SystemEnd();
	return 0;
}
